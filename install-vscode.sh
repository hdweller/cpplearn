#!/bin/bash

# Install the vscode product
# prereq: use the prereq script first to get the repo definition on the system

sudo apt install apt-transport-https
sudo apt update
sudo apt install code # or code-insiders

